{
  "name": "graphql-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "node build.js && mkdir -p dist/schema && cp -R src/schema/*.graphql dist/schema/ && mkdir -p dist/data && cp -R src/data/*.json dist/data/",
    "dev": "ts-node src/index.ts",
    "start": "npm run build && node ./dist/index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@apollo/server": "^5.2.0",
    "graphql": "^16.12.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^24.10.1",
    "esbuild": "^0.21.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
