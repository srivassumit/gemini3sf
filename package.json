{
  "name": "graphql-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "node build.js && mkdir -p dist/schema && cp -R src/schema/*.graphql dist/schema/",
    "dev": "ts-node src/index.ts",
    "start": "npm run build && node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@apollo/server": "^5.2.0",
    "graphql": "^16.12.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "esbuild": "^0.21.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
